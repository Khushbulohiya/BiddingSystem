#!/usr/bin/python

class User:
	def __init__(self, fname, lname, email, password, mobile, gender, dob):
		self.fname = fname
		self.lname = lname
		self.email = email
		self.password = password
		self.mobile = mobile
		self.gender = gender
		self.dob = dob